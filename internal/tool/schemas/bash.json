{
  "type": "object",
  "properties": {
    "command": {"type": "string", "description": "Shell command to execute via 'sh -c'"},
    "timeout": {"type": "integer", "description": "Timeout in seconds (0 = use default 30s)", "minimum": 0}
  },
  "required": ["command"],
  "additionalProperties": false
}
