{
  "type": "object",
  "properties": {
    "path":   {"type": "string", "description": "Absolute or relative file path"},
    "offset": {"type": "integer", "description": "0-based line offset (0-based, inclusive)", "minimum": 0},
    "limit":  {"type": "integer", "description": "Max lines to read (0 = all remaining)", "minimum": 0}
  },
  "required": ["path"],
  "additionalProperties": false
}
